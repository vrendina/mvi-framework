<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.victorrendina.mvi.sample">

    <application
            android:allowBackup="false"
            android:label="@string/app_name"
            android:supportsRtl="false"
            android:name=".MviSampleApplication"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning,MissingApplicationIcon">
        <activity android:name="com.victorrendina.mvi.sample.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".counter.CounterActivity"/>
        <activity android:name=".list.SampleListActivity"/>
        <activity android:name=".resetables.ResettableActivity"/>
    </application>

</manifest>